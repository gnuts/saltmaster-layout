{% set map = salt['grains.filter_by']({
'Debian': {
  'pkgs': ['heartbeat'],
  'confdir': '/etc/heartbeat',
  'servicename': 'heartbeat'
},
}, grain='os', merge=salt['pillar.get']('heartbeat:lookup')) %}



