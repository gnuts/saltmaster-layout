{% set map = salt['grains.filter_by']({
  'Debian': {
    'pkgs': [ 'quagga' ],
    'servicename': 'quagga',
    'confdir': '/etc/quagga',
    'debian' : True,
    'logdir' : '/var/log/quagga',
},
}, grain='os', merge=salt['pillar.get']('quagga:lookup')) %}
